corgea_yaml_version: 1 # important for child policy to have the same version
policies:
  - policy_type: "BLAST"  # Required: Type of policy. Must be one of ["BLAST", "false_positive", "fix"]
    description: "this handle only db migration files, so just check around that logic."  # Required: Description of the policy
    cwes: [200, 89]  # Optional: List of CWE (Common Weakness Enumeration) codes related to this policy

